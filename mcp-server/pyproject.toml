[project]
name = "mcp-server"
version = "0.1.0"
description = "MCP Server with FastMCP and PrefabUI visual tools"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "fastmcp[apps]>=3.2.2",
    "python-dotenv>=1.2.2",
]
